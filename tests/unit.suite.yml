# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - WPCLI
        - Asserts
        - \Helper\Unit
    step_decorators: ~
    config:
        WPCLI:
            path: "%WP_ROOT_FOLDER%"
            throw: false
